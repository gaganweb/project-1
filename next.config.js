/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  basePath: '',  // Update the basePath to match your Vercel subdomain
  assetPrefix: '',  // Update the assetPrefix accordingly
}